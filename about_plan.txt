//
 먼저 tkinter를 이용한 대략적인 UI를 구현한 다음에 내용을 끌어오고 적용하는거는 나중에 적용을 하자
UI를 구현하는 것은 버튼에 따른 기능을 뺀 나머지 모든 기능을 구현하는 것을 목표로 한다.

UI 구현을 마친 후에는 API 정보를 끌어와서 적용하는 것을 목표로 한다.
그래프도 적용할 수 있으면 적용을 한다.

//
 STL 과제 마무리 하는대로 tkinter를 사용한 UI 구현 시작
UI의 구현 정도는 이전과 동일
버튼 클릭에 따른 커맨드 처리는 하지 않는다
아직 API를 불러와서 자료를 관리하고있지 않기 때문

//
엑셀에서 검색 가능한 동네 이름을 가져와서 리스트 박스에 넣는 것 부터
그걸 토대로 동네 이름을 검색하면 x 값과 y 값을 맞게 넣고 API 정보를 가져오도록 한다.
서리 정보에 관한 것도 경도와 위도 값을 넣어 가져오도록 한다.

정보를 가져오면 먼저 간단 정보 보다는 세부 정보를 먼저 구현한다.
이쪽은 텍스트만 출력하기 때문에 그래프 보다는 구현이 쉽기 때문

지도에 관한 부분은 tkinter 윈도우 안에 웹 브라우저를 띄우게 하여 미세미세 페이지를 출력할 것인데
지금.. 음.. 동네에 대한 정보를 바로 띄울 수가 있을라나? 스크립트 내부를 만져서?
여기까지는 잘 모르겠다.
하여튼 지도는 그렇게 출력할거임 ㅡㅡ

//
생각해보니 미세먼지 정보 뿐만 아니라 날씨랑 주식도 구현을 해놔야겠구나..
이 또한 세부 정보에 관한 것만 구현을 하도록 합시다 껄껄.

//
http랑 엘레멘트 트리로 xml파일 읽어와서 미세먼지 세부 정보부터 처리를 하고
그 다음은 날씨 정보, 그 다음은 주식 정보로 구현을 한다.

미세먼지 정보의 경우 측정소가 동읍면 마다 위치하지는 않기 때문에 어떻게 할 지 생각을 해보았는데,
일단.. 처음으로 생각한 것은 동읍면으로 검색이 안 된다면 먼저 시군구로 검색을 한번 더 해보고 없으면
그냥 측정소 정보가 없다고 출력을 하는 방법..

그리고 두 번째 방법은 c++ 연동을 해서 파이썬으로부터 시군구에 속하는 모든 동읍면의 정보를 보낸 후에
위도 경도를 계산하여 가장 근접한 측정소의 정보를 출력한다.
이 경우 c++ 연동의 경우도 해결을 할 수가 있어서 일석이조이기는 하나..
아직 c++ 연동을 제대로 공부하지 않아서 어떤식으로 이 여러 데이터를 보내야 할 지 생각을 해볼 필요가 있다.

만일 세부 정보를 출력하는 것을 모두 구현하면 겜엔진으로 넘어갈 자격이 충분하다. 허허

//
어휴 캔버스가 좀 까다롭네
텍스트를 좌측 정렬하는 것은 포기를 하고 그냥 각 칸의 중앙에 가도록 하자
이 정보 출력하는 것을 함수로 변경하고
미세먼지 부터 구현을 해놓은 후에 시간이 된다면 날씨에 대한 정보까지 출력을 하도록 하자
주식은.. 뭐.. 천천히 해도 되지 않을까시라 껄껄

//
3.9 버전으로 낮췄으니까.. 오른쪽에 지도 넣고..
주식 정보 불러와서 출력하게 만들고..
그래프도 출력하게 만들고..
이미지..? 이미지는 미세먼지 예상 이미지 넣으면 되지 않을라나..

//
이번주 주말까지 포함을 해서 지도 그냥 미세미세 넣고
그래프 관해서 미세먼지는 현재 시간까지 변화
날씨는 오늘 날씨로 넣고
이미지는 버튼에 넣고
c/c++은.. 몰라 정 없으면 스트링 합치는걸로 넣고
아, 주식. 주식도 구현을 해야지..

//
미세먼지에 대한 간단 정보이다.
이전 10 시간에 대한 미세먼지 그래프를 화면에 출력한다
버튼에 따라 초미세먼지, 아황산 등등을 출력하는 것은 추가적으로 구현하는 것으로 생각하겠다.

날씨에 대한 간단 정보이다.
하루 기온에 대한 그래프를 화면에 출력한다.
습도, 강수량 등등을 출력하는 것을 추가적으로 구현하는 것으로 생각하겠다.

주식에 대한 정보이다.
주식.. 넥슨게임즈, 펄어비스, 크래프톤, 넷마블, 데브시스터즈 정도?
일단 데이터 불러와서 데이트 순으로 정렬하고 간단 정보는 종가 그래프 생성
상세 정보는 가장 최근 데이터로 정보들 출력

//
cef와 thread를 사용하여 지도를 오른쪽에 넣는다.
우선적으로 미세미세 지도를 출력한다.
날씨에 대한 지도는 추가적으로 구현하는 것으로 생각하겠다.

//
이미지는 로고 타이틀에 넣을 것이다.
HAMBOGOGA에서 O 부분에 눈을 넣을 것이다.

//
c/c++ 연동에 대한 내용이다.
날짜에 대한 연산을 위하여 c++을 사용할까 생각중이다.

//
텔레그램 봇에 대한 내용이다.
미세먼지 정왕동
날씨 정왕동
주식 넥슨게임즈
처럼 검색할 정보와 검색값을 입력하면 이에 대한 값을 텔레그램 봇이 보내도록 하면 될 것 같다.

//
일단 만들던 주식 정보 파싱부터 하고
주식 세부 정보 출력까지 구현한 다음에 그래프에 대한 구현을 마친다.
그래프까지 구현을 하였으면 타이틀 로고 작업한 다음에 이미지로 넣고
타이틀 버튼 그 튀어나와있는 느낌 없앨 수 있는걸로 알고있어서 그거 수정하자.
그거까지 수정한 다음에는 위에 적은대로 텔레그램 봇 연동하면 스언어 프로젝트는 마무리.

